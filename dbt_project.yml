name: 'pjm_dash'
version: '1.0.0'
profile: 'pjm_dash'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
seed-paths: ["seeds"]
clean-targets: ["target", "dbt_packages"]

models:
  pjm_dash:
    staging:
      +materialized: view
      +schema: staging
    dimensions:
      +materialized: table
    marts:
      +materialized: incremental
      +unique_key: hour_beginning_utc
